model:
  backbone: resnet18
  head: mlp
  feat_dim: 128
  syncbn: true
criterion:
  name: supcon
  temp: 0.5
optimizer:
  name: sgd
  learning_rate: 0.5
  learning_rate_prototypes: 0.01
  momentum: 0.9
  weight_decay: 0.0001
dataset:
  name: cifar10
seed: 0
